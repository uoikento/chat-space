.wrapper 
 .side-bar
  .header
   .header__user
    = current_user.name
   %ul.header__lists
    %li.list
     = link_to new_group_path do 
      = fa_icon 'pencil-square-o', class: 'icon'
    %li.list 
     = link_to edit_user_path(current_user) do 
      = fa_icon 'cog', class: 'icon'
  .groups
   - current_user.groups.each do |group|
    .group
     = link_to root_path(group) do
      .group__name
       = group.name
      .group__message
       メッセージはまだありません。
 .chat
  .header
   .left-header
    .left-header__title
     グループ名  
    %ul.left-header__members
     Member:
     %li.member
      メンバー
     %li.member
      メンバー
   .right-header
    .right-header__button
     = link_to "Edit", edit_group_path(current_user) 
  .messages
   .message
    .up-message
     .up-message__user-name
      名前
     .up-message__date
      2020/01/01
    .low-message
     .low-message__content  
      メッセージ
   .message
    .up-message
     .up-message__user-name
      名前
     .up-message__date
      2020/01/01
    .low-message
     .low-message__content  
      メッセージ   
  .form
   %form.new-message
    .input-box
     %input.content{placeholder: "text"}
      %label.image
       = fa_icon 'image', class: 'icon'

       %input.hidden{type: 'file'}
    
    
    %input.send{type: 'submit', value: 'Send'}
